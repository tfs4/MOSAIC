# Tested with Python 3.10, PyTorch 2.x, CUDA 12 (see README for the exact environment
# used for the paper). Install PyTorch first, following https://pytorch.org/get-started/locally/
torch>=2.1
torchvision>=0.16
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
Pillow>=10.0
tqdm>=4.66
matplotlib>=3.7
seaborn>=0.13

# Patch extraction from the AMLC Zarr stores (scripts/prepare_patches.py)
zarr>=2.16
numcodecs>=0.12

# Optional: tools/download_datasets_gdrive.py
gdown>=5.0
